# Railway Environment Configuration Template
# Copy this to Railway's environment variables section

# Application Settings
APP_NAME="Luxe Hair Studio"
APP_ENV=production
APP_KEY=base64:YOUR_APP_KEY_HERE
APP_DEBUG=false
APP_TIMEZONE=UTC
APP_URL=https://your-app-name.railway.app

# Database - PostgreSQL (Primary Database)
# Railway will automatically provide these via service variables:
# DB_CONNECTION=pgsql
# DB_HOST=${PGHOST}
# DB_PORT=${PGPORT}
# DB_DATABASE=${PGDATABASE}
# DB_USERNAME=${PGUSER}
# DB_PASSWORD=${PGPASSWORD}

# MongoDB Configuration (Services/Catalog Database)
# Set these in Railway after creating MongoDB service:
DB_MONGO_HOST=${MONGODB_HOST}
DB_MONGO_PORT=${MONGODB_PORT}
DB_MONGO_DATABASE=luxe_hair_catalog
DB_MONGO_USERNAME=${MONGODB_USERNAME}
DB_MONGO_PASSWORD=${MONGODB_PASSWORD}
MONGODB_URI=${MONGO_URL}

# Cache & Sessions (Redis)
# Railway will provide these when Redis service is added:
CACHE_DRIVER=redis
SESSION_DRIVER=redis
REDIS_HOST=${REDIS_HOST}
REDIS_PASSWORD=${REDIS_PASSWORD}
REDIS_PORT=${REDIS_PORT}

# Queue Configuration
QUEUE_CONNECTION=redis

# Mail Configuration (Update with your email service)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@luxehair.com
MAIL_FROM_NAME="Luxe Hair Studio"

# File Storage
FILESYSTEM_DISK=public

# Security Settings
SANCTUM_STATEFUL_DOMAINS=${APP_URL}
SESSION_DOMAIN=.railway.app

# Laravel Optimization
OPTIMIZE_CLEAR_CACHE=true

# Additional MongoDB Settings
MONGODB_DSN=${MONGO_URL}

# Session Configuration
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_SAME_SITE=lax

# Broadcasting (if using WebSockets)
BROADCAST_DRIVER=log
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# Logging
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error